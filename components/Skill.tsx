import Image from "next/image"
import Link from "next/link"

interface Props {
  id: string
  name: string
  link: string
  description: string
}

const Skill: React.FC<Props> = ({id, name, link, description}) => {
  return (
  <div>
    
  </div>    
  )
}

export default Skill